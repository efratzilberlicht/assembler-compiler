#include<string.h>
#include <stdlib.h>
#include "first_pass.h"

//void analize_2_second_pass(char* line, int index);
//void encode_line(char* line, int index, int len_op);
//void  encode_operand(char* line, int* index, char* kind, word* w);
//int immediate_address(char* line, int index, char* kind, word* w);
//int direct_register_address(char* line, int index, char* kind, word* w);
//int relative_address(char* line, int index, char* kind, word* w);
//int direct_address(char* line, int index, char* kind, word* w);
//void add_ext_word(symbol* temp);
//void add_word_number(word_number* wn);
//void add_word(word* w, int index);
//int convert_to_d(char* num);
//int create_external_file(char* file_name);
//int create_entry_file(char* file_name);
//int create_object_file(char* file_name);
