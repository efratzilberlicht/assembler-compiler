#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include "memory_map.h"

void initialize_line(char* line);

//int is_it_label(char* line, int index);
//int advance_space(char* line, int index);
//int empty_or_comment_line(char* line, int index);
//int exsist_symbol(char* name);
//void initialize_word(word* w);
//int is_operation(char* str);
//void free_data_table();
//void free_code_table();
//void free_symbol_table();
//void free_externals_list();
//int return_label(char* line, char* label, int* index);